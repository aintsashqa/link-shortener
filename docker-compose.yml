version: "3.8"

services:

  redis:
    image: bitnami/redis
    ports:
      - 6379:6379
    environment:
      - ALLOW_EMPTY_PASSWORD=yes

  mongodb:
    image: mongo
    ports:
      - 27017:27017
